# v5 固件产物说明

这里故意不提交预编译 `.bin`、有效 `sdkconfig` 或私有构建覆盖文件。

ESP32 固件二进制会固化以下部署信息：

- 云端视频 UDP 目标地址和端口；
- 遥测/心跳 UDP 目标地址和端口；
- 本地热点 SSID 与 WPA2 口令。

把这样的二进制提交到公共仓库会泄露基础设施信息与接入凭据。请按上级目录 [README.md](../../README.md) 在可信机器上注入配置并构建，部署包通过受控渠道分发。
