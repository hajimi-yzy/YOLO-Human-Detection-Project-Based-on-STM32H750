dependencies:
  espressif/esp_hosted:
    rules:
    - if: target in [esp32p4, esp32h2]
    version: ~2
  espressif/esp_wifi_remote:
    rules:
    - if: target in [esp32p4, esp32h2]
    version: '>=0.10,<2.0'
  idf: '>=4.4.1'
  json_parser:
    version: 1.0.3
version: 1.0.0
